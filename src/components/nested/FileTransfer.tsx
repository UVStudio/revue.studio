import React from 'react';
import { UserState } from '../../features/user/userSlice';
import { Box } from '@mui/material';

const FileTransfer = ({ userState }: { userState: UserState }) => {
  return <Box>FileTransfer</Box>;
};

export default FileTransfer;
